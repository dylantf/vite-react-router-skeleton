Starter kit for Vite + React + React-Router

Install dependencies:

```sh
$ npm i
```

To run dev server:

```sh
$ npm dev
```

Browse to http://localhost:3000

Recommended VSCode extensions:

- ESLint
- Prettier
